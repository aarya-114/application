# Local development uses SQLite and needs no PostgreSQL server.
DATABASE_URL="file:./dev.db"
SESSION_SECRET="replace-with-a-long-random-secret"

# Production/Vercel: set DATABASE_URL to your PostgreSQL connection string
# and SESSION_SECRET to a private random value in Vercel's environment settings.
# Do not put production credentials or secrets in this file.
